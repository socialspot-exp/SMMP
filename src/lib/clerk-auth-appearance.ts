/** Shared `<SignIn />` / `<SignUp />` styling to sit inside `AuthCard`. */
export const clerkAuthAppearance = {
  variables: {
    colorPrimary: "#f97f24",
    borderRadius: "0.625rem",
  },
  elements: {
    rootBox: "w-full",
    card: "shadow-none bg-transparent",
  },
};
